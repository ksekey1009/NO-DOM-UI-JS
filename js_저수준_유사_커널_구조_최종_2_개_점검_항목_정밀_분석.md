이 문서는 「JS 저수준 유사 커널구조 1차 설계안」의 마지막에 명시된 두 가지 점검 항목을, 설계 의도·엔진 내부 동작·구현 시 파급 효과의 관점에서 AI 아키텍트 시점으로 분해·정제한 분석 문서이다. 원문의 의미를 축약하지 않으며, 판단 근거와 구조적 귀결을 명확히 드러내는 것을 목표로 한다.

---

## 1. V8 바이트코드 정렬 확인

### 원문 요지

현재 설계된 **16슬롯(64바이트) 고정 정렬 + 단일 Uint32Array 수렴 구조**가, 2026년 기준 V8 인터프리터(Ignition) 환경에서 **추측성 최적화(Speculative Optimization)** 를 실패 없이 지속적으로 유지할 수 있는 구조인가를 점검해야 한다는 항목이다.

### 설계 의도의 분해

이 항목의 본질은 “빠른가?”가 아니라, **"언제까지 빠른 상태를 유지할 수 있는가"** 에 있다.

V8의 최적화 파이프라인은 다음과 같은 전제를 가진다.

- 값의 타입이 예측 가능해야 한다
- 메모리 접근 패턴이 반복 가능해야 한다
- 바이트코드 레벨에서 분기와 형태가 안정적이어야 한다

사용자 설계는 이 세 가지를 동시에 겨냥한다.

- 모든 데이터는 Uint32Array → 타입 단일화
- 모든 UI 객체는 16슬롯 → 구조 단일화
- 모든 접근은 고정 오프셋 → 패턴 단일화

이는 V8 입장에서 **"추측이 필요 없는 코드"** 에 가깝다.

### 64바이트 정렬의 실제 의미

16슬롯 × 4바이트 = 64바이트는 단순한 숫자 선택이 아니다.

- 현대 CPU의 L1 캐시 라인 크기와 정확히 일치
- 하나의 UI 블록 접근 = 하나의 캐시 라인 로드
- 필드 접근 순서가 바뀌어도 캐시 미스 확률이 증가하지 않음

V8은 JS 레벨에서 캐시 라인을 직접 인식하지는 않지만,

- TypedArray + 연속 인덱스 접근
- 고정 stride 라는 조건이 맞춰질 경우, 실제 머신 코드에서는 **예측 가능한 메모리 로드 시퀀스** 로 변환된다.

즉, 이 설계는 Ignition → TurboFan 전환 여부와 무관하게, **인터프리터 단계에서도 이미 하드웨어 친화적인 실행 형태**를 강제한다.

### Speculative Optimization 실패 가능성 검토

이 구조에서 추측이 실패할 수 있는 지점은 오직 세 가지뿐이다.

1. 슬롯에 다른 타입이 섞일 경우
2. 슬롯 의미가 런타임에 변할 경우
3. 동일 오프셋이 서로 다른 의미로 재사용될 경우

설계안은 이 세 가지를 모두 금지한다.

- Uint32Array 외 접근 없음
- 슬롯 의미는 문서로 고정
- 오프셋은 상수화 대상

결론적으로, 이 점검 항목은 **위험 요소 탐색**이 아니라 **이미 성립한 설계의 안전성 확인 체크리스트**에 가깝다.

---

## 2. 비트 마스킹 오버헤드 검증

### 원문 요지

32비트 값을 16/16 혹은 그 이하로 분할하여

- Child / Brother
- Focus / Root 등의 정보를 관리하는 방식이, JS 엔진의 **SMI(Small Integer)** 최적화 범위 내에서 가장 효율적인가를 검증해야 한다는 항목이다.

### 이 항목의 핵심 질문

이 질문은 단순히 “비트 연산이 빠른가?”가 아니다. 진짜 질문은 다음이다.

> 이 연산이 **V8이 좋아하는 정수 연산의 형태** 인가?

### SMI 관점에서의 정합성

V8은 특정 조건에서 정수를 SMI로 취급한다.

- 31\~32비트 정수
- 부동소수점으로 승격되지 않을 것이라는 확신

`>>>`, `&`, `|`, `<<` 같은 비트 연산자는 V8에게 다음을 명확히 전달한다.

- 이 값은 정수다
- 이 값은 계속 정수로 유지된다

따라서

- 32비트 필드
- 그 내부를 다시 비트 분할 하는 구조는 **SMI 유지에 가장 유리한 패턴**이다.

### Child / Brother 통합 필드의 의미

전통적인 객체 트리는 다음을 필요로 한다.

- 객체 참조
- null 체크
- 포인터 추적

현재 설계는 이를 모두 제거한다.

- Child와 Brother를 하나의 정수에 압축
- 마스크 + 시프트로 즉시 분해
- 메모리 간접 참조 없음

이는 알고리즘적 관점에서도 의미가 크다.

- 트리 순회가 포인터 기반이 아니라 **인덱스 기반**
- 분기 비용이 조건문이 아니라 **비트 연산**

### 오버헤드의 실체

비트 연산의 비용은 다음과 같다.

- AND / SHIFT : 1\~2 사이클 수준
- 브랜치 미스 : 수십 사이클
- 객체 참조 : 캐시 미스 가능성 동반

즉, 이 구조는 **비트 연산을 늘리는 대신, 예측 불가능한 비용을 제거하는 선택**이다.

### JS 엔진 관점 결론

- 이 비트 분할 방식은 SMI 최적화 범위를 벗어나지 않는다
- 오히려 V8이 가장 안정적으로 최적화할 수 있는 형태다
- 오버헤드는 존재하지만, 제거한 비용에 비하면 무시 가능하다

---

## 종합 결론

이 문서의 마지막 두 점검 항목은 “위험한 실험을 하고 있는가?”를 묻는 것이 아니다.

오히려 다음을 확인하는 절차이다.

- 설계가 **엔진의 본성과 충돌하지 않는가**
- 하드웨어·JIT·인터프리터의 교집합 위에 정확히 올라가 있는가

분석 결과, 두 항목 모두

- 구조적으로 이미 정합성이 확보되어 있으며
- 구현 시 규칙만 어기지 않는다면
- 성능 붕괴 가능성은 극히 낮다

이 지점부터는 최적화의 문제가 아니라 **규율의 문제**다. 설계를 지키는 한, 엔진은 배신하지 않는다.

---

## 다음 단계 실행 로드맵 (분석 기반 · 과정 생략 없음)

이 섹션은 앞선 두 점검 항목 분석을 **실제 구현·검증 단계로 전환**하기 위한 절차적 로드맵이다. 각 단계는 서로를 전제로 하며, 건너뛰면 의미가 붕괴된다.

---

### 1단계: 메모리 맵 불변성 고정 (Invariants Lock)

**목적**: V8의 추측 실패 원인을 원천 제거

1. 모든 슬롯 오프셋을 상수(Enum)로 선언
   - 숫자 리터럴 직접 접근 전면 금지
   - 의미 변경 가능성 차단
2. 각 슬롯의 의미를 주석이 아닌 "문서적 계약"으로 고정
   - X/Y, W/H, State 비트 등 재해석 금지
3. 개발 단계에서도 구조 변경 시 반드시 메모리 맵 문서부터 수정

이 단계가 끝나면, 메모리 레이아웃은 **코드가 아니라 규격**이 된다.

---

### 2단계: TypedArray 접근 패턴 정제 (Access Pattern Freezing)

**목적**: Ignition 인터프리터 단계에서도 안정적인 바이트코드 생성

1. 동일 슬롯 접근은 항상 동일한 함수/루프 위치에서 수행
2. 조건에 따라 다른 슬롯을 접근하는 패턴 제거
3. 인덱스 계산식은 루프 외부에서 선계산

이 단계의 결과는 V8 입장에서

> "이 배열은 항상 같은 방식으로 읽힌다" 라는 확신을 주는 것이다.

---

### 3단계: 비트 필드 연산 규율 확정 (Bit Discipline)

**목적**: SMI 유지 + TurboFan 인라이닝 안정성 확보

1. 모든 상태 갱신은 반드시 비트 연산자 사용
   - 산술 연산 후 비트화 금지
2. 상위/하위 비트 분리는 매크로화
   - 직접 마스크 값 사용 금지
3. 32비트 초과 연산 경로 원천 봉쇄

이 규율이 깨지는 순간, 전체 시스템은 Float64 경로로 이탈할 수 있다.

---

### 4단계: 단일 진입점 디스패치 검증 (Dispatch Stability Test)

**목적**: Megamorphic 진입 방지

1. execute() 내부 분기는 오직 Command\_Code 기반
2. 문자열, 객체, 함수 참조 분기 금지
3. Sub-manager 호출은 숫자 ID 기반 고정 테이블 사용

이 단계는 TurboFan에게

> "이 함수는 항상 같은 형태로 호출된다" 라는 메시지를 전달한다.

---

### 5단계: 핫패스 / 콜드패스 분리 선언 (Hot/Cold Path Declaration)

**목적**: 인라이닝 예산 초과 방지

1. 프레임마다 실행되는 경로를 문서상 Hot-path로 지정
2. 예외, 디버그, 희귀 이벤트는 Cold-path로 강제 분리
3. Cold-path는 별도 함수로 분리하되 메모리 접근 규칙은 동일 유지

여기서 중요한 점은 **기능 분리이지 구조 분리가 아니라는 것**이다.

---

### 6단계: 엔진 관점 검증 시나리오 실행 (Engine-Level Validation)

**목적**: "이론적으로 빠름"을 "실제로 무너지지 않음"으로 전환

1. 프레임 반복 실행 후 성능 편차 측정
2. UI 수 증가에 따른 선형성 유지 여부 확인
3. 장시간 실행 시 GC 개입 여부 확인

이 검증에서 중요한 것은 절대 성능 수치가 아니라 **형태의 유지**다.

---

### 7단계: 위반 탐지용 체크리스트 고정 (Regression Guard)

**목적**: 미래의 자기 파괴 방지

- 새로운 슬롯을 추가했는가?
- 비트 연산 대신 산술 연산을 썼는가?
- execute()에 분기가 추가되었는가?
- TypedArray 외 구조가 생겼는가?

이 질문 중 하나라도 "예"라면, 설계 위반이다.

---

## 8단계: 코드 레벨 강제 장치 삽입 (Implementation Guards)

**목적**: 설계를 "지키려는 의지"가 아니라 **어길 수 없게 만드는 구조**로 전환

이 단계부터는 문서가 아니라 코드가 규율을 강제한다.

### 8-1. 슬롯 접근 매크로 고정

- 모든 TypedArray 접근은 직접 인덱싱 금지
- 반드시 상수 기반 매크로/함수 경유

의미:

- 잘못된 슬롯 접근은 즉시 코드 리뷰 단계에서 드러남
- 오프셋 변경이 발생하면 컴파일 타임 수준에서 파급

---

### 8-2. 비트 연산 전용 헬퍼 계층 도입

- 상태 비트 조작은 전용 함수 집합으로만 수행
- AND / OR / SHIFT 연산의 직접 사용 금지

의미:

- 비트 폭 침범, 잘못된 마스크 사용 원천 차단
- SMI 이탈 가능성 중앙 통제

---

### 8-3. execute() 내부 형태 고정

- switch(Command\_Code) 외 분기 추가 금지
- 조건문(if)은 사전 정제 단계에서만 허용

의미:

- Megamorphic 진입 구조 물리적 봉쇄
- TurboFan 인라이닝 안정성 유지

---

### 8-4. 개발 단계 어설션(Assertion) 삽입

- 개발 모드에서만 활성화되는 경량 체크 추가
- 슬롯 범위, 비트 마스크 정합성 검사

주의:

- 런타임 예외 처리용이 아님
- "이 구조는 틀렸다"를 즉시 드러내는 장치

---

## 9단계: 성능 붕괴 조기 탐지 장치 (Early Deopt Detection)

**목적**: 느려진 뒤 원인 분석이 아니라, **느려지기 직전 탐지**

1. 프레임 단위 실행 시간 분산 기록
2. TypedArray 외 객체 생성 감시
3. execute() 호출 형태 변화 감시

이 단계의 목표는 수치를 보는 것이 아니라 **형태 변형을 감지**하는 것이다.

---

## 10단계: 규율 위반 시 대응 프로토콜 고정

**목적**: "왜 느려졌지?"를 묻지 않기 위한 절차

- 성능 하락 발견 시 기능 추가 중단
- 최근 변경 사항을 메모리 맵 → 비트 규율 → 진입점 순으로 역추적
- 원인 해결 전까지 새로운 슬롯/상태 추가 금지

이 프로토콜은 기술 규칙이 아니라 **개발 규칙**이다.

---

## 최종 정리

이 시점에서 시스템은 다음 상태에 도달한다.

- 최적화는 선택이 아니라 **구조적 귀결**
- 느려짐은 버그가 아니라 **규율 위반의 증상**
- 성능 문제는 튜닝 대상이 아니라 **설계 붕괴 신호**

이 문서와 함께 구현된 코드는, 엔진이 아니라 **개발자를 통제하는 시스템**이 된다.

여기까지가 1번 경로의 완결이다.

---

## PCUI JS 코드 정밀 스캔 결과 기반 추상화 단계 (UI Class Abstraction)

### 1. 현재 구조의 핵심 성격 요약

PCUI.0.0.1.js는 단일 파일 안에 다음 성격들이 **강하게 결합**되어 있다:

- 시스템 커널(system\_) : 이벤트 디코딩, 슬롯 관리, 렌더 스케줄링
- UI 객체(ui\_) : 좌표·부모자식·포커스·렌더 정보 동시 보유
- 입력 장치 처리 : pointer/mouse/touch 비트마스크 FSM
- IME 로직 : 문자 조합 상태 머신

이 결합은 *빠르지만*, 추상화 관점에서는 **불안정 영역을 전파**시키는 구조다.

---

### 2. 불안정하지만 지금 제거하면 안 되는 영역 (봉인 대상)

아래 영역은 구조적으로 위험하지만, 현재 성능·동작을 지탱하고 있으므로 **추상화 바깥으로 밀어내고 유지**한다.

- system\_.DMEHP / listening / bitmask 판별 블록
- PSA / PSABM 배열 기반 포인터 슬롯 처리
- Dirty Rect 계산(DRRAC) 및 rect counter 로직
- IME 내부 조합 상태(#c\_, #v\_, #c2 등)

이 영역들은 \*\*"Kernel Internal"\*\*로 정의하고 외부 접근을 차단한다.

---

### 3. UI 클래스들의 실제 책임 재분해

현재 ui\_ 클래스는 다음 역할을 동시에 수행한다:

1. 공간 데이터 보유 (coord / area)
2. 트리 구조 관리 (parent / children)
3. 입력 반응 주체 (hit-test, focus)
4. 렌더링 대상 데이터 제공

→ 이 네 역할은 **하나의 클래스에 존재하면 V8 최적화와 유지보수 모두에서 불리**하다.

---

### 4. 1차 추상화 목표 (구현 변경 없음)

**목표**: 로직을 바꾸지 않고 *역할 경계만 만든다*.

#### 4-1. 추상 베이스: UIBase

- UID 보유
- parent / children 트리 구조만 담당
- 좌표·렌더·이벤트 로직 없음

UIBase는 "존재"만 표현한다.

#### 4-2. 공간 계층: UILayoutNode

- UIBase를 상속
- get\_coord / get\_area / get\_sum\_rect만 보유
- Dirty Rect 계산에는 관여하지 않음

좌표는 **읽기 전용 인터페이스**로 노출

#### 4-3. 시각 계층: UIRenderNode

- UILayoutNode 상속
- renderer\_가 소비하는 최소 데이터만 제공
- draw(ctx)는 선언만 하고 실제 구현은 renderer\_에 둔다

렌더링은 *위임*한다.

#### 4-4. 상호작용 계층: UIInteractiveNode

- UIRenderNode 상속
- hit-test, focus, pointer reaction 플래그만 보유
- system\_ 비트마스크와 직접 연결되지 않음

이 계층은 \*\*"이 UI가 반응할 수 있는가"\*\*만 표현한다.

---

### 5. system\_ 과 UI 사이의 새로운 계약

- system\_은 UI를 **직접 조작하지 않는다**
- system\_은 오직 UID + 비트 상태만 전달
- UI 계층은 자신의 상태를 *조회*만 한다

이로써 system\_은 **완전한 커널**, UI는 **순수 객체 그래프**가 된다.

---

### 6. 지금 당장 하지 않는 것 (중요)

- TypedArray 전환
- 메모리 매니저 도입
- 성능 최적화 재작성

이 모든 것은 **추상화 안정화 이후 단계**다.

---

### 7. 이 단계가 끝났을 때 얻는 것

- 코드 동작 100% 동일
- 불안정 영역 격리 완료
- UI 클래스가 "역할" 단위로 사고 가능
- 다음 단계(메모리 구조 재편, 커널 분해)가 *기계적으로 가능*

이 시점에서 PCUI는 더 이상 하나의 거대한 파일이 아니라, **분해 가능한 시스템**이 된다.

---

## PCUI 메모리 기반 UI 커널 설계 종합 정리 (누락 없는 고정본)

이 문서는 PCUI를 **객체 기반 UI 시스템이 아닌, 메모리 기반 단일 스레드 UI 커널**로 정의하기 위한 최종 설계 정리본이다.

본 설계에서 UI는 인스턴스가 아니며, 객체도 아니고, 상태를 소유하지 않는다. UI는 오직 **사전에 할당된 메모리 슬롯을 해석하는 규약**으로만 존재한다.

---

## 1. UI의 존재 방식: 객체가 아닌 슬롯

- UI는 new로 생성되지 않는다.
- UI pool에 **사전 할당된 슬롯**이 존재할 뿐이다.
- 각 UI는 단 하나의 정수 idx로만 식별된다.
- idx는 포인터이며, 메모리 주소에 준하는 의미를 갖는다.

UI 생성은 객체 생성이 아니라 **idx 할당**이다. UI 제거는 소멸이 아니라 **링크 해제 및 재사용 가능 상태 전환**이다.

---

## 2. UI Pool 구조와 참조 그래프

UI Pool은 고정 크기의 메모리 영역이며, 다음과 같은 포인터 구조를 사용한다.

- idx            : 자기 자신을 가리키는 포인터
- pidx           : 부모 UI의 idx
- firstChild     : 첫 번째 자식 UI idx
- nextBrother    : 형제 UI idx

이 구조는 배열 기반 인접 리스트이며, 객체 참조나 배열 중첩을 사용하지 않는다. 트리는 데이터 구조일 뿐이며, 객체 그래프가 아니다.

### firstChild 규칙 (중요)

- `firstChild === 0`

  - 해당 UI는 자식을 갖지 않는다
  - children 체인은 존재하지 않는다

- `firstChild > 0 && firstChild !== idx`

  - 하나 이상의 자식 UI가 존재함을 의미한다
  - `firstChild`는 child(ren) 체인의 시작점(head)이다

### nextBrother 규칙 (중요)

- `nextBrother === 0`

  - 형제가 존재하지 않음을 의미한다
  - 마지막 자식이거나 단독 노드임을 나타낸다

- `nextBrother > 0 && nextBrother !== idx`

  - 다음 형제 UI가 존재함을 의미한다
  - 형제 체인은 단방향 연결 리스트로 구성된다

이 규칙에 따라 별도의 child count, 배열, 플래그 없이도 **자식/형제 존재 여부와 순회 종료 조건**을 단일 비교로 판단할 수 있다.

---

## 3. UI 헤더 포맷: type / pidx 결합 슬롯

UI 노드는 하나의 32bit 헤더 워드를 갖는다.

- 상위 16bit: UI type (class/type id)
- 하위 16bit: pidx (parent index)

```
[ type:16 ][ pidx:16 ]
```

이 헤더는 다음을 동시에 제공한다.

- 이 UI가 **어떤 해석기를 통과해야 하는지** (type)
- 이 UI가 **트리 상 어디에 위치하는지** (pidx)

### pidx의 의미 규칙 (중요)

- `pidx === 0`

  - 부모가 존재하지 않음을 의미한다
  - 단, **참조 값으로서의 의미만 소멸**하며 슬롯 자체는 유효하다

- `pidx === idx`

  - 해당 UI가 **최상위(root) UI임을 명시적으로 선언**한다
  - 시스템 레벨 루트 노드 또는 독립 트리의 기준점으로 사용된다

- `pidx > 0 && pidx !== idx`

  - 일반적인 부모-자식 관계를 의미한다

이 규칙에 따라 `pidx`는 단순한 null 대체가 아니라, **트리 위상(state)을 인코딩하는 필드**로 작동한다.

pidx와 type은 동일한 접근 빈도와 동일한 불변 성격을 가지므로 반드시 같은 워드에 결합되어야 한다.

---

## 4. UI Type의 의미

UI type은 클래스가 아니다. 상속이나 다형성을 의미하지 않는다.

UI type은 다음을 정의하는 **해석 규약 ID**이다.

- 메모리 레이아웃 계약
- 허용된 연산 집합
- 대응되는 Manager 및 exec 디스패치 루틴

즉, type은 opcode에 가깝다.

---

## 5. Manager 중심 설계 (커널)

모든 UI 관련 메모리는 Manager가 전면 관할한다.

Manager의 특성:

- 인스턴스화 금지
- static field / static method로만 구성
- 메모리 섹터 소유
- 파라미터 섹터 관리
- class/type 기반 디스패치 수행

UI는 Manager를 통해서만 조작된다.

---

## 6. 호출 모델: 파라미터 기록 + void 실행

UI 관련 모든 호출은 다음 흐름을 따른다.

1. XXXXManager.setParameters(...)
   - 파라미터를 전달하지 않는다
   - 파라미터 섹터에 값을 기록한다
2. XXXXManager.ref()
   - void parameter 실행
   - memorySector에서 classId / callerId / idx를 읽는다
3. dispatchTable[type].exec()
   - offset + idx 기반으로 실제 데이터를 읽어 처리한다

JS의 싱글 스레드 특성으로 인해 모든 호출은 자연스럽게 직렬화된다. 락이나 동기화는 필요 없다.

---

## 7. UI 클래스의 실체 (빈 껍데기)

개발자가 사용하는 UI 클래스는 다음 특성을 가진다.

- 상태를 소유하지 않는다
- 필드를 갖지 않는다
- 메서드는 Manager 호출만 수행한다

이 클래스들은 **문법적 손잡이(handle)** 역할만 하며, 실행 의미는 전부 커널에 있다.

상속은 구조 상 필요하지만, 의미적 기능은 없다.

---

## 8. 텍스트 영역 설계

텍스트 UI는 일반 UI와 해석 방식이 완전히 다르므로 분리된 영역으로 취급한다.

원칙:

- UI pool에는 텍스트 컨테이너 노드만 존재
- 실제 텍스트 데이터는 별도의 TextMemorySector에 존재
- UI 노드는 텍스트 메모리의 anchor 역할만 수행

TextMemorySector는 다음을 포함한다.

- codepoint 스트림
- 속성(attribute) 스트림
- 커서 및 선택 영역
- IME 조합 버퍼

IME는 UI 트리를 순회하지 않으며, 텍스트 메모리만 조작한다. UI는 텍스트 내용을 알지 못한다.

---

## 9. 특수 목적 UI 영역

특수 목적 UI는 일반 UI 규약으로 처리하지 않는다.

대상 예:

- 스크롤러
- 슬라이더
- 가상 리스트
- 캔버스
- 복합 입력 컨트롤

이들은 다음 원칙을 따른다.

- UI pool에는 anchor 노드만 존재
- type으로 전용 해석기 분기
- 실제 로직은 전용 Manager + 전용 메모리 영역에서 처리

이들은 UI가 아니라 **소형 서브시스템**이다.

---

## 10. 시스템 경계 선언 (가장 중요)

- UI는 데이터를 소유하지 않는다
- UI는 로직을 실행하지 않는다
- UI는 메모리 해석의 진입점일 뿐이다

텍스트 VM, 특수 UI VM, 렌더러, IME는 모두 UI 커널을 통해서만 연결된다.

이 경계가 깨지면 설계는 즉시 붕괴한다.

---

## 최종 선언

PCUI는 UI 프레임워크가 아니다.

PCUI는:

> **JS 위에 구현된 단일 스레드, 결정적 실행을 갖는 메모리 기반 UI 커널 및 UI 가상 머신이다.**

이 문서는 기능 설명서가 아니라, **이 설계를 절대 깨뜨리지 않기 위한 규율 문서**다.

---

## 11. Focus 모델 설계 (Hit-Test 의존 · Mobile UX 기준)

Focus는 독립적인 상태 머신이 아니다. Focus는 **Hit-Test 결과에 의해 결정되고, Blur 이벤트 이전까지 유지되는 고정 상태**다.

본 설계는 데스크톱 포인터 UX가 아니라 **모바일 UX를 기준**으로 한다.

---

## 11-1. Focus 결정 원칙

- Focus는 반드시 **Hit-Test 성공 결과**로만 설정된다
- 임의 설정, 프로그래밍적 강제 Focus 변경은 허용하지 않는다
- Focus 대상은 단 하나의 UI idx만 가질 수 있다

즉:

> Hit-Test 없는 Focus는 존재하지 않는다

---

## 11-2. Focus 유지 규칙 (Latch Model)

- Focus가 한 번 설정되면
- 다음 조건 중 하나가 발생하기 전까지 **유지된다**

유지 종료 조건(Blur 발생 조건):

1. 명시적인 Blur 이벤트
2. 새로운 Hit-Test 성공으로 다른 UI가 Focus를 획득
3. Focus 대상 UI가 트리에서 제거됨(pidx → 0)

이 모델은 모바일의 터치 UX와 동일하다.

---

## 11-3. Move / Drag 중 Focus 처리

- Move / Drag 중에는 Hit-Test를 수행하지 않는다
- 최초 down 시점의 Hit-Test 결과가 Focus를 결정한다
- Drag 중 포인터가 영역을 벗어나도 Focus는 유지된다

이는 모바일 터치 인터랙션의 핵심 규칙이다.

---

## 11-4. Multi-pointer 환경에서의 Focus

- Focus는 전역적으로 단일하다
- 복수 포인터가 존재해도 Focus는 하나만 유지된다
- 새로운 포인터의 Hit-Test는 Focus를 **선점하지 않는다**

Focus 변경은 항상 **명시적 Hit-Test 성공 이벤트**에 의해서만 발생한다.

---

## 11-5. Focus와 UI Type의 관계

- Focus 가능 여부는 UI type의 해석 규약에 포함된다
- 모든 UI type이 Focus를 가질 수 있는 것은 아니다
- type별로 Focus 허용 여부가 명시적으로 정의되어야 한다

---

## 11-6. 커널 관점에서의 Focus 저장 방식

- Focus 상태는 UI 노드에 저장하지 않는다
- 커널 전역 Focus 레지스터(단일 idx 슬롯)로 관리한다
- Focus 변경은 Manager 전용 루틴을 통해서만 수행된다

이를 통해 UI 메모리 레이아웃의 불변성이 유지된다.

---

## 11-7. 설계 선언

Focus는 이벤트가 아니라 **상태 고정(latched state)** 이다.

- Hit-Test → Focus 설정
- Blur 이전까지 유지
- 모바일 UX와 동일한 사용자 경험 제공

이 규칙은 PCUI 전반에 걸쳐 절대 위반되어서는 안 되는 **입력/상호작용 설계의 핵심 규율**이다.

---

## 12. UI 생성 상한 규율 (스크립팅 기반 무한 생성 금지)

PCUI에서는 스크립팅을 통한 무한 UI 생성이 **구조적으로 불가능**해야 한다. 이는 성능 문제가 아니라 **커널 안전성 규율**이다.

---

## 12-1. 기본 원칙

- UI는 "생성"되는 개념이 아니다
- UI는 사전에 확보된 UI pool 슬롯을 **할당받을 뿐**이다
- 따라서 UI 수는 항상 **명시적인 상한을 가진다**

"조금 많다" 수준은 허용되지만, 상한이 없는 증식은 설계 위반으로 간주한다.

---

## 12-2. Type별 Hard Cap 규칙

- 모든 UI type은 반드시 **hard cap**을 가진다
- hard cap은 동시에 살아 있을 수 있는 최대 UI 수를 의미한다
- 이 값은 설계 문서에 명시적으로 고정된다

hard cap 초과 시:

- 새로운 UI 할당은 실패한다
- 개발/디버그 환경에서는 assert 또는 명시적 경고를 발생시킨다
- 릴리즈 환경에서는 noop 또는 안전한 대체 동작을 수행한다

이 상황은 런타임 예외가 아니라 **설계 위반 탐지 이벤트**다.

---

## 12-3. 스크립팅 환경에 대한 제약

- 스크립트는 UI pool을 직접 조작할 수 없다
- UI 생성 API는 Manager를 통해서만 접근 가능하다
- 반복문, 재귀, 이벤트 콜백을 통한 무제한 UI 생성은 구조적으로 차단된다

스크립트는:

- UI "요청"만 할 수 있고
- 실제 할당 여부는 커널이 결정한다

---

## 12-4. 설계적 효과

이 규율을 통해 다음이 보장된다.

- 메모리 폭주 불가
- UI 트리 폭발 불가
- 프레임 단위 성능 예측 가능
- 악의적/실수 기반 스크립트로부터 커널 보호

UI 고갈은 버그이며, 무한 증식은 기능이 아니라 **차단 대상**이다.

---

## 12-5. 선언

PCUI에서 UI는 자원이자, 무한한 논리 객체가 아니다.

UI 개수는 항상 통제 가능해야 하며, 통제되지 않는 생성 경로는 커널 설계 차원에서 허용되지 않는다.

---

## 13. 데이터 레퍼런스 공개 및 운영 태도 선언

본 프로젝트는 서비스가 아니다. 지원 약속, 로드맵 제공, 지속적 업데이트 보장을 전제로 하지 않는다.

PCUI는 연구 및 개인 활용을 목적으로 한 **데이터 레퍼런스**이며, 공개 이후에도 설계자는 이를 가지고 자유롭게 실험하고 다룬다.

---

## 13-1. 공개의 목적

- 설계를 공유하기 위함
- 참고 자료로 활용 가능하게 하기 위함
- 이해 가능한 사람에게만 사용되도록 하기 위함

사용자 수 확대, 커뮤니티 활성화, 기능 요청 수렴은 목적이 아니다.

---

## 13-2. 사용자 기대에 대한 선제적 제한

다음과 같은 기대는 본 레퍼런스의 범위에 포함되지 않는다.

- 업데이트 일정 제공
- 기능 추가 요청 반영
- 사용법 질의 응답
- 문제 해결 지원

이는 무관심이나 방치가 아니라, 프로젝트 성격에 부합하는 명시적 경계 설정이다.

---

## 13-3. 오픈소스 사용자 그룹에 대한 입장

본 레퍼런스는:

- 업데이트를 기다리는 사용자 그룹
- 방향 결정에 관여하려는 사용자
- 어부지리형 기여자

를 전제로 설계되지 않았다.

설계 방향은 문서로 이미 고정되어 있으며, 외부 요구에 의해 변경되지 않는다.

---

## 13-4. 책임의 위치

- 레퍼런스 제공자는 구조와 규율을 명시한다
- 사용자는 이를 이해하고 선택적으로 활용한다
- 수정, 확장, 조정의 책임은 사용자에게 있다

설계자는 프로젝트 운영자나 관리자가 아니다.

---

## 13-5. 선언

PCUI는 의무가 아닌 자유 연구 대상이다.

공개 이후에도 설계자는 스트레스를 감수하지 않으며, 본 레퍼런스를 기반으로 한 사용 결과에 대해 직접적인 책임을 지지 않는다.

이는 회피가 아니라, 데이터 레퍼런스로서의 정체성을 유지하기 위한 조건이다.
